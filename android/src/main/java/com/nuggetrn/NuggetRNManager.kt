package com.nuggetrn

// No specific manager logic needed for a Hello World module.
// This file can be kept minimal or even removed if not strictly required by the build process
// or if the module registration does not rely on it.
// For now, keeping a minimal structure.

object NuggetRNManager {
  // If registerNuggetModule was used for specific setup, it's likely not needed now.
  // internal fun registerNuggetModule(nuggetModule: NuggetRNModule) {
    // No-op
  // }
} 