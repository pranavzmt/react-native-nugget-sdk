module.exports = {
  dependency: {
    platforms: {
      ios: {},
      android: {}, // projects are grouped into "platforms"
    },
    assets: [], // stays the same
    hooks: {}, // stays the same
  },
}; 